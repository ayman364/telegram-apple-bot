services:
  - type: worker
    name: telegram-apple-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    env:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
